# Mac交叉编译Dockerfile
# 注意：这个方法比较复杂，推荐使用GitHub Actions

FROM python:3.9-slim

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    build-essential \
    git \
    wget \
    && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /app

# 复制项目文件
COPY . .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install pyinstaller

# 构建应用
RUN pyinstaller --onefile --name "联通网盘管理器" wopan_gui.py

# 输出目录
VOLUME ["/app/dist"]

CMD ["echo", "Mac应用已构建完成，请查看/app/dist目录"]
